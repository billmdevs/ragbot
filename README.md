# Simple RAG-based bot

Building a simple Retrieval Augmented Generation (RAG) based bot in Python. It allows users to ask questions about multiple PDF documents. It extracts text from PDFs, creates embeddings, builds a vector store, and uses a conversational retrieval chain to answer user queries based on the document content.
